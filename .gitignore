# Global ignores (e.g., OS-specific and IDE files)
.DS_Store # macOS specific hidden directory
Thumbs.db # Windows specific hidden file
.env      # General environment file if placed in the root

# Backend specific ignores (for files/folders inside the 'backend' directory)
backend/node_modules/ # Node.js dependencies for backend
backend/.env          # Backend's environment variables (critical for security)
backend/*.log         # Any log files generated by the backend

# Frontend specific ignores (for files/folders inside the 'frontend' directory )
frontend/node_modules/ # Node.js dependencies for frontend
frontend/.env          # Frontend's environment variables (if any, like API keys)
frontend/build/        # Common build output directory for React
frontend/dist/         # Alternative common build output directory for React/Vite
frontend/*.log         # Any log files generated by the frontend
